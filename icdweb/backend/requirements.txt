# Exact-pinned backend dependencies for reproducible container builds.
# (Artifact determinism comes from the icdgen core library, installed
# separately in the Dockerfile and pinned in icdgen/requirements.txt.)
fastapi==0.136.3
uvicorn[standard]==0.49.0
python-multipart==0.0.32
pydantic==2.13.4
