# Exact-pinned dependencies.
#
# Output must be byte-reproducible. A floating version (>=) could pull an
# updated ReportLab/python-docx/openpyxl on a future rebuild and change
# generated bytes, invalidating certification evidence tied to a hash. These
# are the versions the deterministic baseline hashes were verified against.
# After any bump, re-verify the baseline (see AI_README "Determinism
# contract").
PyYAML==6.0.1
jsonschema==4.26.0
python-docx==1.2.0
reportlab==4.4.10
Jinja2==3.1.6
openpyxl==3.1.5
